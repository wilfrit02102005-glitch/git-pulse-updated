services:
  gitpulse:
    build: .
    container_name: gitpulse
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
      - FLASK_DEBUG=0
    volumes:
      - gitpulse_logs:/app/logs
    restart: unless-stopped

volumes:
  gitpulse_logs:
